<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="refresh" content="0; url={{ redirect.to | url }}">
  <link rel="canonical" href="{{ site.url }}{{ redirect.to }}">
  <meta name="robots" content="noindex, follow">
  <title>Redirecting to {{ redirect.title }}</title>
</head>
<body>
  <p>This page has moved to <a href="{{ redirect.to | url }}">{{ redirect.title }}</a>.</p>
</body>
</html>
